apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "e-budget.fullname" . }}-test-connection"
  labels:
    {{- include "e-budget.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "e-budget.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
